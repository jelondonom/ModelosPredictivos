# Notebook Objective:

# Carry out the creation of three classification models (Naive Bayes, Decision Tree, KNN) to identify the type of business to which a salesperson belongs according to their socioeconomic variables (Age, Female, Years, Certificates, Feedback, Salary).

# Subsequently, the goal is to determine which model is better at performing binary classification based on its evaluation metrics. This will then be used to generate clusters / groupings based on correlations between variables.

# The goal is to generate 2 hypothetical cases that do not initially exist in the dataset to determine which grouping they would belong to and the probability of that classification being true or not.

# And to find additional insights by conducting a detailed analysis of multiple variables within this dataset

# Dataset Description:

# 'Sales_Rep' = Advisor's ID

# 'Business' = The department to which they belong

# 'Age' = Advisor's age

# 'Female' = Binary variable indicating female gender when equal to 1

# 'Years' = N
